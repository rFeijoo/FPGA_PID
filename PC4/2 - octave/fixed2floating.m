% Authors: Rafael Feijó Leonardo% Date: 09 nov 2020% This function receives a 8bit fixed point number (1 bit signal + 5 bit mag + 2 bit fraction)% and converts it to its 32bit floating point representation.function [f] = fixed2floating(bin)f = 0;if (length(bin(1)) != 8)    #error("ERROR - binary must be 8 bits");else    decimal  = bin2dec(bin(2:6));        fraction = bitget(bin2dec(bin), 2:-1:1);        f = decimal + fraction(1) * 2^(-1) + fraction(2) * 2^(-2);endifendfunction